package examples.behavioral_pattern.memento

/**
 * MEMENTO PATTERN
 * This design pattern create a memento to save current state of object (save/undo method in object).
 * It gets memento state from CareTaker - this is a helper manager to save state and name in a hash map (same Flyweight pattern).
 */

fun main() {

}